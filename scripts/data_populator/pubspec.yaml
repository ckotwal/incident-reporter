
name: data_populator
description: A script to populate the database with sample incident data.
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  http: ^1.2.1
  random_string: ^2.3.1
  dart_geohash: ^2.1.0
